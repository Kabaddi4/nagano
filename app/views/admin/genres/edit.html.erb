<div class="container">
  <div class="row">
    <h4>ジャンル編集</h4>
  </div>

  <div class="row">
  <%= form_with model: @genre,:url => {:action => :update}, lolal: true do |f| %>
    <%= f.label "ジャンル名" %>
    <%= f.text_field :name, class: "mx-5" %>
    <%= f.submit "変更を保存", class: "btn btn-success" %>
  <% end %>
  </div>
</div>