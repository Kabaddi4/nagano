<h2>商品一覧</h2>


<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>価格</th>
      <th>商品説明</th>
      <th></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
<% @items.each do |item| %>
  <tbody>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.price %></td>
      <td><%= item.introduction %></td>
      <td><%= link_to '詳細', admin_item_path(item.id) %></td>
    </tr>
  </tbody>
</table>
<% end %>

<%= link_to "商品追加", new_admin_item_path %><br>
<%= link_to "顧客一覧", admin_customers_path %>
