<table>
    <thead>
        <th>商品</th>
        <th>個数</th>
    </thead>

    <tbody>
        <% @cart_items.each do |cart_item| %>
        <tr>
            <td><%= cart_item.item %></td>
            <td><%= cart_item.amount %></td>
        </tr>
        <% end %>
    </tbody>
</table>